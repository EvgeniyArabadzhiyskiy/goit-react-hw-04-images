import styled from 'styled-components';
import { border, color,display,flexbox,grid,layout,position,space, typography } from 'styled-system';

const Box = styled.div`
  ${space}
  ${layout}
  ${color}
  ${display}
  ${typography}
  ${flexbox}
  ${position}
  ${border}
  ${grid}
  
`;

export default Box;
